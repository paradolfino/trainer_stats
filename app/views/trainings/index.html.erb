<h3>Trainings Log</h3>
<hr/>
<table>
    <thead>
        <tr>
            <th>Member Name</th>
            <th>Company Name</th>
            <th>Time Logged</th>
        </tr>
    </thead>
    <tbody>
        <% @trainings.each do |t| %>
            <tr>
                <td><%= t.member %></td>
                <td><%= t.company %></td>    
                <td><%= t.created_at.in_time_zone("Central Time (US & Canada)").strftime("%m/%d/%Y at %H:%M") %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to "Log a New Training", root_path %>