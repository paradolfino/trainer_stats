<%= form_for ([@log, @training]) do |t| %>
    <%= t.label :member %>
    <p><%= t.text_field :member, placeholder: "(Optional)" %></p>
    <%= t.label :company %>
    <p><%= t.text_field :company, placeholder: "(Optional)" %></p>
    <%= t.label "Trainer" %>
    <p><%= select_tag(:trainer, options_for_select(["", "Chris V.", "Mac S.", "Audra S.", "Aaron V."])) %></p>
    <p><%= t.submit "Submit" %></p>
<% end %>