<h3 class="text-center">Inactive Logs (<%= @inactive %>/<%= @total %>) [<small>with a total of (<%= @total_trainings %>) trainings logged.</small>]</h3>

<table class="table text-center">
    <thead>
        <tr>
            <th>Date Created</th>
            <th>Log Title</th>
            <th># of Trainings</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% @logs.each do |t| %>
            <tr>
                <td><%= t.created_at.strftime("%m/%d/%Y") %></td>
                <td><%= link_to t.title, log_path(t) %></td>
                <td><%= t.trainings.count %></td>
                <td><%= link_to fa_icon("edit", text: "Edit"), edit_log_path(t) %></td>
            </tr>
        <% end %>
    </tbody>
</table>
<div class="options">
    <%= link_to fa_icon("search", text: "View Active"), logs_path %>
</div>